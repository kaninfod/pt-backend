FROM ruby:2.4.1

RUN apt-get update -yqq \
  && apt-get install -yqq --no-install-recommends \
   mysql-client libmagic-dev graphicsMagick imagemagick libjpeg-dev exiftool

RUN mkdir /app
WORKDIR /usr/src/app
COPY Gemfile* ./
RUN bundle install
COPY . .
